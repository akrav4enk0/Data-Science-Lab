# Results

This folder contains report-supporting summary artifacts.

- `fib_latency_summary.csv`: aggregated latency results used in the report.
- `answers/` (not tracked): per-run model outputs generated locally when running benchmarks.
